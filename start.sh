#!/usr/bin/env bash
set -u

echo "mcpServer is an unsupported internal reference snapshot, not a deployable application." >&2
echo "Read REFERENCE_STATUS.md. The former launcher and demo-credential path are retired." >&2
exit 78
